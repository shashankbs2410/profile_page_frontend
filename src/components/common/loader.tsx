import classes from "../../styles/componentStyles/common/loader.module.css";

const LoaderComponent = () => {
  return <div className={classes.sending_animation} />;
};

export default LoaderComponent;
